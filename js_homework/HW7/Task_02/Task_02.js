/* Проверка функции: y = 1/x + sqrt(x) .*/function CalcFunction(x) {    return ((1 / x) + Math.sqrt(x));}describe("Позитивный тест. Вычисление значение функции из ООФ.", function () {    var inputVar = 4;    var result = 2.25;    var msg = "Вывести результат вычисления y = 1/x + sqrt(x), где x: " + inputVar + " результат " + result;    it(msg, function () {        var res = CalcFunction(inputVar);        expect(res).toBe(result);    });});describe("Позитивный тест. Вычисление значение функции в краевой точке x=0 .", function () {    var inputVar = 0;    var result = Infinity;    var msg = "Вывести результат вычисления y = 1/x + sqrt(x), где x: " + inputVar + " результат " + result;    it(msg, function () {        var res = CalcFunction(inputVar);        expect(res).toBe(result);    });});describe("Позитивный тест. Вычисление значение функции из не ООФ.", function () {    var inputVar = -1;    var result = NaN;    var msg = "Вывести результат вычисления y = 1/x + sqrt(x), где x: " + inputVar + " результат " + result;    it(msg, function () {        var res = CalcFunction(inputVar);        expect(res).toBeNaN();    });});describe("Позитивный тест. Вычисление значение функции в точке Infinity.", function () {    var inputVar = Infinity;    var result = Infinity;    var msg = "Вывести результат вычисления y = 1/x + sqrt(x), где x: " + inputVar + " результат " + result;    it(msg, function () {        var res = CalcFunction(inputVar);        expect(res).toBe(result);    });});